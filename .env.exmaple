# ========================
# Database Configuration
# ========================
POSTGRES_DB=orefox
POSTGRES_USER=orefox
POSTGRES_PASSWORD=orefoxpw
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ========================
# Django Settings
# ========================
# ⚠️ Change this in production!
DJANGO_SECRET_KEY=dev-please-change

# Set to 0 in production
DJANGO_DEBUG=1

# Comma-separated hostnames/domains
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# ========================
# MinIO / S3 Storage
# ========================
# ⚠️ Change these in production!
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Default bucket for document uploads
MINIO_BUCKET=documents

# MinIO service endpoint (keep http://minio:9000 for Docker Compose)
MINIO_ENDPOINT=http://minio:9000